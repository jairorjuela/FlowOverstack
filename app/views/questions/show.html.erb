</br>
</br>
<div class="container-fluid">
  <div class="row">
    <div class="col-md-9">
      <div class="jumbotron">
        <h2>Pregunta del usuario <strong><%= @question.user.email     %></strong></h2>
        <h3><%= @question.title %></h3>
          <p><%= @question.body %></p>
          <% if signed_in? %>
            <% if current_user.id == @question.user.id %>
              <%= link_to 'Editar', edit_question_path(@question) %>
              <%= link_to 'Borrar', question_path(@question), method: :delete %>
            <% end %>
          <% end %>
      </div>
    </div>
  </div>
</div>

<% @question.comments.each do |comment| %>
  <div class="container-fluid">
    <div class="row">
      <div class="col-md-9">
        <h5>Comentario de la Pregunta:</h5>
        <div class="text-right">
          <p style="font-size:18px;"> <%= comment.body %> <strong style="color:red; font-size:14px;"> Usuario: <%= comment.user.email %></strong></p>
          <hr class="col-md-12" style="width: 97%; color: black; height: 1px; background-color:black; margin-bottom:2px; margin-top:2px;" />
        </div>
      </div>
    </div>
  </div>
<%end %>

<div class="container-fluid">
  <div class="row">
    <div class="col-md-9">
      <% if signed_in? %>
        <h3>Comentar la Pregunta</h3>
          <div class="container">
            <h4>Hola  <strong style="color:orange; font-size:19px;"><%= User.find_by(id: current_user.id).email %></strong> realiza tu comentario a la pregunta</h4>
            <%= form_for [@question, @question.comments.build] do |f| %>
            <div class="col-md-11">
              <div class="form-group">
                <%= f.text_area :body, class: "form-control", placeholder: "Agregar Comentario"%>
              </div>
            <%= f.submit "Comentar", class: "btn btn-primary" %>
            <% end %>
          </div>
          </div>
      <% else %>
      <div class="text-right">
        <%= link_to "Comentar Pregunta", user_session_path, class: "btn btn-primary" %>
      </div>
      <% end %>
    </div>
  </div>
</div>


<hr class="col-md-12" style="width: 72%; color: black; height: 4px; background-color:black; margin-bottom:10px; margin-top:15px;" />

<div class="container-fluid">
  <div class="row">
    <div class="col-md-9">
        <h1><span class="label label-info">Respuestas</span></h1>
    </div>
      <% @question.answers.each do |answers| %>
      <div class="row-fluid">
          <div class="col-md-9">
            <div class="jumbotron">
            <div class="container">
              <p style="font-size:18px;"> <%= answers.body %> </p>
            </div>
            <div class="text-right">
              <p style="font-size:18px;"><strong style="color:red; font-size:14px;"> Usuario: <%= answers.user.email %></strong></p>
            </div>
            <hr class="col-md-12" style="width: 72%; height: 2px; background-color:black; margin-bottom:5px; margin-top:5px;" />
            <div class="container">
              <h7 style="font-size:18px;"> COMENTARIO </h7>
            </div>
            <div class="container-fluid">
              <div class="row">
                <div class="col-md-9">
                  <% if signed_in? %>
                    <h3>Comentar la Respuesta</h3>
                      <div class="container">
                        <h4>Hola  <strong style="color:orange; font-size:19px;"><%= User.find_by(id: current_user.id).email %></strong> realiza tu comentario a la Respuesta</h4>
                        <%= form_for [@question, @question.comments.build] do |f| %>
                        <div class="col-md-11">
                          <div class="form-group">
                            <%= f.text_area :body, class: "form-control", placeholder: "Agregar Comentario a la respuesta"%>
                          </div>
                        <%= f.submit "Comentar", class: "btn btn-primary" %>
                        <% end %>
                      </div>
                      </div>
                  <% else %>
                  <div class="text-right">
                    <%= link_to "Comentar Respuesta", user_session_path, class: "btn btn-primary" %>
                  </div>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        </div>
      <%end %>
    </div>
  </div>
</div>




<hr class="col-md-12" style="width: 72%; color: black; height: 2px; background-color:black; margin-bottom:5px; margin-top:5px;" />

<div class="container-fluid">
  <div class="row">
    <div class="col-md-9">
      <% if signed_in? %>
        <h3>Responde la Pregunta</h3>
          <div class="container">
            <h4>Hola  <strong style="color:orange; font-size:19px;"><%= User.find_by(id: current_user.id).email %></strong> Responde a la pregunta</h4>
            <%= form_for [@question, @question.answers.build] do |f| %>
            <div class="col-md-11">
            <div class="form-group">
              <%= f.text_area :body, class: "form-control", placeholder: "Respuesta"%>
            </div>
            <%= f.submit "Responder", class: "btn btn-primary" %>
            <% end %>
          </div>
          </div>
      <% else %>
      <div class="text-right">
        <%= link_to "Responder", user_session_path, class: "btn btn-primary" %>
      </div>
      <% end %>
    </div>
  </div>
</div>
